# Core deep-learning stack (install matching CUDA build separately)
# torch==2.2.0        # CUDA 11.8 build (see README); install a matching PyTorch 2.2 wheel first
# torchvision==0.17.0
SimpleITK==2.2.1
scikit-image==0.19.3
scipy==1.7.3
pandas==1.3.5
Cython==3.0.11
healpy==1.15.2
pyfftw==0.12.0
numexpr==2.7.3
openpyxl
opencv-python-headless
tqdm
wandb
torchmetrics==1.3.1   # VIF metric for real-data eval

# ---------------------------------------------------------------------------
# torch_radon is REQUIRED (DRR simulation + geometry). It is NOT on PyPI and
# must be built from source with the compatibility patch shipped in this repo:
#   torch-radon_fix/torch-radon_fix.patch
# See README.md -> "Environment / torch_radon" for the exact offline build.
# ---------------------------------------------------------------------------
